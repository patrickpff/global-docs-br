import { describe, it, expect } from "vitest";
// import { validateOAB } from "../validate.js";

describe("validateOAB", () => {
    it("should be true", () => { return true }) 
    // it("should validate a correct OAB number", () => {
    //     const oab = "MG123456";
    //     expect(validateOAB(oab)).toBe(true);
    // });

    // it("should invalidate an incorrect OAB number", () => {
    //     const oab = "MG12X45X";
    //     expect(validateOAB(oab)).toBe(false);
    // });

    // it("should invalidate an OAB number with wrong state code", () => {
    //     const oab = "XX123456";
    //     expect(validateOAB(oab)).toBe(false);
    // });

    // it("should accept an OAB number with few digits", () => {
    //     const oab = "MG1234";
    //     expect(validateOAB(oab)).toBe(true);
    // });

    // it("should invalidate an OAB number without numeric part", () => {
    //     const oab = "MG";
    //     expect(validateOAB(oab)).toBe(false);
    // });

    // it("should validate an OAB number with formatting characters", () => {
    //     const oab = "MG-123456";
    //     expect(validateOAB(oab)).toBe(true);
    // });
});